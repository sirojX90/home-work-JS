let s;
let a = 6.3;
let b = 8.6;

s = (9 * a**2 * b) - (27 * a**2 * b**2) + (15 * b**2);

console.log(s.toFixed(2));

a = 4.5;
b = 9.6;

s = (9 * a**2 * b) - (27 * a**2 * b**2) + (15 * b**2);

console.log(s.toFixed(2));