let m, s;

m = 3.456;

s = Math.round(m) + (m.toFixed(2) - Math.round(m));

console.log(s);

m = 5.678;

s = Math.round(m) + (m.toFixed(2) - Math.round(m));

console.log(s);

m = 7.5;

s = Math.round(m) + (m.toFixed(2) - Math.round(m));

console.log(s);